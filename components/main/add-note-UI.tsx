import React from "react";

import Input from "../UI/input";
import InputWithButton from "../UI/input-with-button";

type Props = { children: JSX.Element | JSX.Element[] };

function AddNoteUI({ children }: Props) {
	return <div></div>;
}

export default AddNoteUI;
